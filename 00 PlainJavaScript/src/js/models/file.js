(function (app) {
    app.File = function () {
        var name,
            id,
            size;

        return {
            name: name,
            id: id,
            size: size
        };
    };
})(app);